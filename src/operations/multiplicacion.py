def multiplicar(x: float, y: float) -> float:
    """
    Calcula la multiplicación de dos números.

    Args:

        x: El primer número (flotante).
        y: El segundo número (flotante).

    Returns:
        La multiplicación de x y y.
    """
    return x * y
